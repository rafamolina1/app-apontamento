// O NOVO CÓDIGO (EM next.config.js)

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ... (Mantenha quaisquer outras configurações que você tinha aqui)
};

module.exports = nextConfig;
